name: docker-ci-test

on:
  repository_dispatch:
    types: [docker-ci-test]

jobs:
  docker-ci-test:
    runs-on:
      - self-hosted
      - linux
      - aws
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0 # checkout full repo with all tags

      - name: Test docker github action
        uses: ./.github/actions/update-tracker-stories
